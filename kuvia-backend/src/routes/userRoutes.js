const express = require('express');
const router = express.Router();

// Rotas de utilizador podem ser adicionadas aqui no futuro
// Exemplo: router.get('/', userController.listUsers);

module.exports = router;
