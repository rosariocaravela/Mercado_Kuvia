const express = require('express');
const router = express.Router();

// Rotas de produto podem ser adicionadas aqui no futuro
// Exemplo: router.get('/', productController.listProducts);

module.exports = router;
